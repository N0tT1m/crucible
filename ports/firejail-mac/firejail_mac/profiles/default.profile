# Default safe-ish baseline.
private-tmp
blacklist ${HOME}/.aws
blacklist ${HOME}/.ssh
blacklist ${HOME}/.config/gh
blacklist ${HOME}/Library/Application Support/Code/User
read-only ${HOME}/Library/Keychains
